{
  "name": "mntn",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "sass": "sass --watch src/scss:dist/css",
    "serve": "browser-sync start --server dist --files 'dist/*.html, dist/css/*.css'",
    "start": "npm-run-all --parallel sass serve"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^3.0.3",
    "nodemon": "^3.1.7",
    "npm-run-all": "^4.1.5",
    "sass": "^1.79.4"
  }
}
